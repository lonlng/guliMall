spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:33061/gulimall_wms?serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: gulimall-ware
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 7300
  servlet:
    encoding:
      charset: utf-8

coupon:
  user:
    name: 张三
    age: 25
