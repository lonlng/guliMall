spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:33061/gulimall_pms?serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    driver-class-name: com.mysql.jdbc.Driver
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
